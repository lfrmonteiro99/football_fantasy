# Development Dockerfile — React dev server with hot reload
FROM node:18-alpine

WORKDIR /app

COPY package.json package-lock.json ./
RUN npm ci

COPY . .

EXPOSE 3000

# React dev server with API proxy (defined in package.json)
CMD ["npm", "start"]
